## Installation

```./run```

Then jenkins will be available on port 8090


NB ./clean will fail initially but give you the id's of container that you need
to stop / rm

Remove the container then run ./clean again

